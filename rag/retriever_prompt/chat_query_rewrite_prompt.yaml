messages:
  - role: system
    content: "You are a query rewriter that generates better search queries."
  - role: placeholder
    content: "{messages}"
  - role: user
    content: "{input}"
  - role: user
    content: >
      Based on the above conversation, especially the latest messages,
      generate a concise and relevant search query that captures the user's current information need.
